/**
 * @deprecated Dead code — kept only as a tombstone.
 *
 * This component referenced `RecurringPlan` / `BillingCycle` types that were
 * removed when pricing moved to one-time survey packages + coin bundles
 * (see features/pricing/domain/pricing-data.ts). It was never imported
 * anywhere and its broken imports failed `npm run typecheck`.
 *
 * Safe to delete this file entirely.
 */
export {};
